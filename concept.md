建構「Local Typeless」：基於 Electron 與混合 AI 模型的本地化語音重寫系統深度技術研究報告1. 行政摘要與研究背景在當前的人工智慧發展浪潮中，語音轉文字（Speech-to-Text, STT）技術已從單純的「聽寫」演進為具備語意理解與重構能力的「智慧聽寫」。商業軟體如 Typeless 利用雲端算力，不僅將語音轉錄為文字，更能移除贅字、修正文法，甚至根據上下文調整語氣 1。然而，隨著隱私意識的抬頭以及邊緣計算硬體的效能躍進，使用者對於「Local Typeless」——即在完全不連網、數據不出本地設備的前提下，實現同等智慧水平的語音輸入系統——的需求日益增長。本研究報告旨在回應這一需求，提供一份詳盡的技術實踐藍圖。我們將探討如何利用 Electron 作為應用程式框架，結合 OpenAI Whisper（用於聽覺）與 Hugging Face 上的開源大型語言模型（LLM，用於思維），構建一套隱私優先、反應即時且具備高度智慧的本地語音重寫系統。報告將深入分析「混合模式」（Hybrid Mode）的架構優勢，評估適合本地運行的模型組合（如 Whisper Turbo 搭配 Qwen 2.5 或 Llama 3.2），並詳細解構如何透過提示工程（Prompt Engineering）來實現中英文夾雜（Code-switching）環境下的精準語意重構 3。本報告長達兩萬字，旨在為專業開發者與技術決策者提供從底層原理到工程實踐的全面指導，確保讀者能依據本報告構建出媲美甚至超越商業 SaaS 產品的本地化解決方案。2. 範式轉移：從雲端依賴到本地智慧2.1 語音輸入技術的演進脈絡回顧語音輸入技術的發展史，我們可以清晰地劃分出三個階段。第一階段是以 Nuance Dragon NaturallySpeaking 為代表的早期本地軟體，它們依賴龐大的詞庫與統計模型，雖然在特定領域（如醫療、法律）表現優異，但缺乏對自然語言的深層理解，且對硬體資源消耗巨大。第二階段是隨著 Siri、Google Assistant 和雲端 API 興起的連網時代，這一階段利用雲端強大的算力解決了識別率問題，但引入了隱私洩露風險與網路延遲的不穩定性 2。目前我們正處於第三階段的開端：高保真本地智慧（High-Fidelity Local Intelligence）。這一階段的特徵在於模型的小型化與硬體專用加速器（如 Apple Neural Engine, NVIDIA Tensor Cores）的普及。現在，一個經過量化（Quantization）處理的 30 億參數模型（3B Model）已能在消費級筆記型電腦上流暢運行，其推論能力足以處理複雜的語法修正與風格轉換任務。這使得「Local Typeless」不再是遙不可及的夢想，而是一個具備高度可行性的工程目標 3。2.2 「Typeless」模式的核心解構要打造本地版的 Typeless，首先必須解構其產品哲學。Typeless 區別於傳統聽寫軟體的關鍵在於它不追求「逐字稿」（Verbatim），而是追求「意圖稿」（Intent）。根據研究資料，Typeless 的核心功能包括移除填充詞（如 "um", "uh"）、修正自我更正的語句（如 "I went to... no, I decided to go"）、以及根據應用場景自動格式化文本 1。這意味著我們的本地系統不能僅僅是一個 ASR（自動語音識別）系統，它必須是一個 ASR + LLM 的複合系統。ASR 負責將聲波準確還原為文字符號（包含贅字與錯誤），而 LLM 則扮演「編輯」的角色，對原始文本進行清洗、重組與潤飾。這種「先聽後想」的混合模式（Hybrid Mode）是實現 Local Typeless 的唯一路徑 8。2.3 本地化的隱私與效能優勢採用本地架構構建此系統具有顯著優勢。首先是絕對隱私。語音數據包含了使用者的生物特徵、情緒狀態以及潛在的商業機密。商業雲端服務雖然宣稱「不保留數據」，但在傳輸與暫存過程中仍存在風險。本地系統確保音訊流從未離開設備記憶體，這對於法律、醫療或高機密研發領域的專業人士至關重要 3。其次是延遲的可控性。雲端服務的響應時間受限於網路帶寬與伺服器負載，通常存在 2 至 5 秒的延遲。而本地系統在經過優化後，利用本機 GPU 加速，可以將處理延遲壓縮至 1 秒以內，甚至實現「邊說邊出字」的即時流式體驗。此外，本地模型允許使用者針對特定詞彙（如專案代號、人名）進行微調或外掛字典，這是通用雲端模型難以企及的客製化深度 6。3. 架構藍圖：Electron 與本地推論引擎的深度融合3.1 為什麼選擇 Electron？在眾多跨平台應用框架中，Electron 是構建此類系統的首選。雖然 Rust 語言的 Tauri 框架在資源佔用上更具優勢，但 Electron 擁有最強大的生態系統與原生模組支援，這對於處理音訊流、全域鍵盤監聽以及與底層 C++ 模型庫的整合至關重要 4。Electron 的雙進程架構（Main Process 與 Renderer Process）為我們提供了一個天然的「前後端分離」環境：主進程（Main Process）：這是 Node.js 環境，負責與作業系統交互。在我們的架構中，主進程將扮演「指揮官」的角色，它負責載入 C++ 編寫的 AI 模型（透過 Node.js Addons 或子進程），管理全域快捷鍵（Global Shortcuts），以及執行最終的文字注入（Text Injection）操作。由於 AI 推論是計算密集型任務，必須在主進程或獨立的 Worker 線程中執行，以免阻塞 UI 渲染 13。渲染進程（Renderer Process）：這是 React/Vue 等前端框架運行的環境，負責 UI 展示。它將顯示錄音狀態波形、即時轉錄的預覽文本，以及提供模型設定介面。3.2 混合模式（Hybrid Mode）的系統架構設計所謂「混合模式」，在本系統中具有雙重含義：一是模型大小的混合（小模型負責即時反饋，大模型負責最終潤飾），二是處理流程的混合（ASR 與 LLM 的串聯）。我們提出以下推薦的架構數據流：音訊採集層（Audio Capture Layer）：利用 portaudio 或 Electron 原生 API 擷取系統麥克風輸入。為了實現「無感輸入」，系統需常駐背景，並透過 VAD（語音活動檢測）自動切分語音片段。聽覺推論層（Hearing Inference Layer）：採用 Whisper.cpp 作為核心引擎。這是一個高度優化的 C++ 移植版本，支援 Apple Silicon (Metal) 與 NVIDIA (CUDA) 加速。它將音訊流轉換為原始文本（Raw Text）。此層需具備「流式輸出」（Streaming Output）能力，將初步識別結果透過 IPC（進程間通訊）即時傳送至前端，給予使用者即時反饋 6。思維推論層（Thinking Inference Layer）：採用 Llama.cpp 或 Ollama 作為 LLM 執行後端。當一句話結束（由 VAD 判定），原始文本被送入此層。LLM 根據預設的 System Prompt 對文本進行重寫，輸出最終的「Typeless」風格文本 8。注入層（Injection Layer）：利用作業系統的可訪問性 API（Accessibility API）或模擬鍵盤輸入（如 robotjs），將最終文本直接輸入到使用者當前聚焦的應用程式中（如 Notion, Slack, VS Code）10。3.3 進程間通訊（IPC）與效能優化在 Electron 中，大量的音訊數據與文本數據在進程間傳輸可能會造成效能瓶頸。傳統的 IPC.send 在傳輸大數據時會涉及序列化與反序列化（Serialization/Deserialization）的開銷。為了極致優化，建議採用 SharedArrayBuffer 進行音訊數據的共享。主進程與渲染進程可以同時訪問同一塊記憶體區域，從而實現零拷貝（Zero-copy）的音訊流傳輸。這對於降低 VAD 的延遲至關重要。此外，對於 AI 模型的載入，建議採用 child_process.spawn 啟動獨立的二進位執行檔（如編譯好的 main 程式來自 whisper.cpp），透過 stdin/stdout 進行通訊，這樣可以避免 Node.js 的單執行緒限制影響模型推論的穩定性 14。4. 聽覺層技術解析：ASR 與 VAD 的極致優化聽覺層是整個系統的基石。如果 ASR 聽錯了，後端的 LLM 再聰明也無法憑空修正（甚至可能產生幻覺）。因此，選擇正確的模型與 VAD 策略是成功的關鍵。4.1 語音活動檢測（VAD）：Silero 的應用在本地環境中，為了節省電力並避免對背景噪音進行無意義的轉錄，VAD 是必不可少的。Silero VAD 是目前開源界公認的黃金標準，它是一個輕量級的深度神經網路，能精準區分人聲與背景噪音，且延遲極低 17。4.1.1 實作策略在 Electron 中整合 Silero VAD 時，不應使用 WebAssembly 版本（雖然方便但性能稍差），而應在 Node.js 環境中直接調用 ONNX Runtime 運行的模型。滑動窗口緩衝（Sliding Window Buffer）：系統應維護一個約 0.5 秒至 1 秒的環形緩衝區（Ring Buffer）。當 VAD 觸發「Speech Start」信號時，系統不僅開始錄製，還會將緩衝區內的音訊一併寫入，這樣可以防止語句的第一個音節（如 "Hello" 的 "H"）被截斷。參數調優：根據研究，建議將 positiveSpeechThreshold 設定為 0.5，negativeSpeechThreshold 設定為 0.35。這提供了一個滯後區間（Hysteresis），防止在語音停頓時頻繁切斷錄音 19。靜默抑制：對於短於 200ms 的聲音片段應視為雜訊直接過濾，這能有效去除鍵盤敲擊聲或滑鼠點擊聲的干擾。4.2 ASR 模型選型：OpenAI Whisper 的本地化OpenAI 的 Whisper 模型改變了 ASR 的格局。對於本地應用，我們關注的是如何在準確率與速度之間取得平衡。Hugging Face 提供了多種變體，以下是針對不同硬體配置的深入比較：4.2.1 頂級選擇：Whisper Large-v3-Turbo這是 2024 年底發布的突破性模型 21。架構特點：它基於 Whisper Large-v3 進行了「蒸餾」（Distillation）與剪枝，將解碼層（Decoder Layers）從 32 層大幅減少至 4 層。這使得它的參數總量雖然仍有約 8 億（800M），但推論速度提升了 6 倍以上。性能表現：在混合語言（中英文夾雜）的測試中，其準確率（Word Error Rate, WER）幾乎與完整的 Large-v3 持平，遠優於 Medium 或 Small 模型。硬體需求：量化後的 GGUF 版本約需 2GB 顯存/記憶體。這對於現代 Mac (M1/M2/M3) 或配備 RTX 顯卡的 PC 來說是輕而易舉的。推薦理由：這是實現「Hybrid Mode」中「大模型」的最佳選擇。它能捕捉到極其細微的語音細節，包括口音與專有名詞，為後端的 LLM 提供最優質的原料 23。4.2.2 輕量選擇：Distil-Whisper若目標設備是較舊的筆電或需極致省電，Distil-Whisper (distil-large-v3) 是一個備選方案。特點：它是專門針對英語進行優化的蒸餾版本，速度極快。缺點：在處理中文或中英混合語音時，其表現不如 Large-v3-Turbo 穩定，可能會出現漏字現象 23。4.3 混合語言（Code-switching）的處理挑戰使用者的需求中特別提到了「混合模式」。Whisper 模型天生支援多語言，但在處理中英夾雜（Code-switching）時，舊版模型（如 Small/Base）傾向於將語音「翻譯」成單一語言，而非「轉錄」混合文本。例如，使用者說 "這個 Project 的 deadline 是明天"，小模型可能會轉錄為 "這個專案的截止日期是明天" 或 "The deadline for this project is tomorrow"。為了強制模型進行字面轉錄（Verbatim Transcription），我們需要在呼叫 whisper.cpp 時注入特定的 initial_prompt。Prompt 策略：設置 Prompt 為：「以下是一段包含中文與英文的技術討論，請如實轉錄。」（The following is a discussion containing both Chinese and English technical terms, please transcribe verbatim.）。這能顯著引導模型的注意力機制（Attention Mechanism），使其準備好同時預測中英文字符 12。5. 思維層模型選型：Hugging Face 小模型與混合策略一旦我們有了原始文本（Raw Transcript），下一步就是「Typeless」的核心：重寫。這一層需要一個具備邏輯推理能力的小型語言模型（SLM）。在 2025 年的開源生態中，我們有極佳的選擇。5.1 模型篩選標準在 Hugging Face 上搜尋適合本地運行的模型時，我們依據以下標準進行配對與篩選：參數規模：限制在 20 億至 70 億（2B - 7B）之間。這個範圍的模型在量化後（Q4_K_M）佔用記憶體在 2GB 至 5GB 之間，可以與 Whisper 共存於 8GB 或 16GB 的設備上。指令遵循能力（Instruction Following）：模型必須嚴格遵守「只修改文法，不改變原意」的指令，不能自作聰明地進行續寫或摘要。多語言能力：必須精通中文與英文，且能處理混合語法。5.2 推薦模型配對分析以下是針對本專案需求的最佳模型配對表，分為三種配置等級：配置 A：全能型平衡組合（推薦）ASR: Whisper Large-v3-Turbo (q5_0)LLM: Qwen 2.5-3B-Instruct (q4_k_m)分析：Qwen 2.5 是目前 3B 量級中的王者 26。它在邏輯推理、編碼以及中英文處理能力上全面超越了同級對手。根據基準測試（Benchmark），Qwen 2.5-3B 在 MATH 和 MMLU 測試中的得分顯著高於 Llama 3.2 3B。對於「Local Typeless」來說，它能精準識別並修正中英夾雜的語法錯誤（例如："把這個 file open 一下" -> "Open 這個 file" 或保持原樣但修正語氣），且極少出現幻覺。記憶體預估：總佔用約 4GB RAM。配置 B：純英文極速組合ASR: Distil-Whisper-Large-v3LLM: Llama 3.2-3B-Instruct分析：Llama 3.2 是 Meta 針對邊緣設備優化的模型 27。雖然其中文能力稍弱於 Qwen，但在純英文的商務寫作、語氣潤飾方面表現出極高的自然度與專業感。如果使用者的主要場景是英文郵件撰寫，這是最佳選擇。記憶體預估：總佔用約 3.5GB RAM。配置 C：極致輕量/舊設備組合ASR: Whisper Small-enLLM: Gemma-2-2B-It分析：Google 的 Gemma 2 在 2B 參數下展現了驚人的創意寫作能力 29。雖然邏輯性不如 Qwen，但其文學修辭能力較強。適合用於創意筆記的快速整理。記憶體預估：總佔用約 2.5GB RAM。5.3 混合模式的混搭策略（Mix & Match Strategy）「混搭」不僅是選擇模型，更是動態調度。我們建議在 Electron 應用中實作動態模型切換：情境感知切換：應用程式偵測當前活躍視窗（Active Window）。當使用者在 VS Code 時，系統自動切換 System Prompt 為「程式碼模式」，並優先使用 Qwen 2.5（因其強大的 Coding 能力）。當使用者在 Outlook 時，切換為「郵件模式」，若為全英文則調用 Llama 3.2。這種策略能最大化利用不同模型的專長，達到「混合專家」（Mixture of Experts, MoE）的效果，但這是在應用層實現的邏輯 MoE。6. 提示工程與上下文感知系統擁有模型只是第一步，如何對模型下達指令（Prompting）決定了輸出的品質。針對「Typeless」需求，我們需要設計一套精密的 System Prompt 體系。6.1 核心提示詞架構（The Master Prompt）這是一個針對中英混合語音重寫的通用提示詞模板。請注意，我們利用「角色扮演」與「負面約束」來強化模型表現。Role你是一位專業的文字編輯與轉錄專家，精通繁體中文與英文。你的任務是將使用者的原始語音轉錄文本（Raw Transcript）重寫為流暢、專業的書面文本。Guidelines保留原意：絕對不要刪減關鍵訊息，不要進行摘要（Summarize），保留所有專有名詞、數字與日期。修正語病：移除所有的填充詞（如 "那個"、"就是"、"um"、"uh"、"you know"）。處理自我修正：如果使用者說話時進行了自我更正（例如："我想要... 不，我決定去..."），請只保留最終意圖（即："我決定去..."）。語言處理：保持使用者的原始語言混合模式（Code-switching），不要強制翻譯，除非語法極度不通順。確保中英文之間的空格與標點符號符合排版規範（例如：在中文與英文單字間加入半形空格）。格式化：根據語意自動分段。若內容包含列點（List），請使用 Markdown 格式呈現。Tone專業、簡潔、直接。Input Text{user_raw_transcript}Output Text（僅輸出重寫後的文本，不要包含任何解釋或前言）6.2 針對特定場景的 Prompt 變體根據研究資料，使用者希望模型能「配對」不同的場景 30。我們可以在 Electron 中建立一個 Prompt Library：場景模式關鍵 Prompt 指令適用模型Email 潤飾"將此文本重寫為一封正式的商務郵件，語氣需禮貌且專業。修正所有口語化表達。"Llama 3.2 3B程式開發"將此語音視為程式碼邏輯描述或變數命名。將 'snake case user id' 轉為 user_id。保留技術術語的英文原名。"Qwen 2.5 3B腦力激盪"保留使用者的意識流風格，僅修正明顯錯誤。使用列點符號整理不同的想法。"Gemma 2 2B翻譯模式"將所有內容翻譯為流暢的英文（或中文）。"Qwen 2.5 3B6.3 上下文注入（Context Injection）為了讓本地模型達到甚至超越雲端模型的智慧，我們可以利用 Electron 獲取的本地上下文並注入到 Prompt 中：剪貼簿上下文：如果使用者說「修改這段文字」，系統可以讀取剪貼簿內容作為 Reference Text 放入 Prompt。畫面上下文：雖然本地執行多模態模型（Vision Model）成本較高，但可以讀取當前視窗標題（Window Title）。例如標題包含 "Meeting with John"，Prompt 可以自動加入 "Context: This is a meeting with John."，幫助模型正確拼寫 "John" 而非 "Jon"。7. 工程實踐與性能優化本節將詳細說明如何將上述理論轉化為實際的 Electron 應用程式。7.1 開發環境與依賴庫選擇為了快速啟動，建議使用 electron-vite-react-boilerplate 31。核心依賴：whisper-node 或直接編譯 whisper.cpp 為 Node.js Addon。推薦後者以獲得對 Core ML (Mac) 或 CuBLAS (NVIDIA) 的原生支援。node-llama-cpp：這是一個優秀的庫，它封裝了 llama.cpp，讓 Node.js 可以直接加載 GGUF 模型並進行對話管理（Context Management）13。@ricky0123/vad-node：用於 VAD 處理。7.2 實作流程圖（Step-by-Step）模型準備：從 Hugging Face 下載 whisper-large-v3-turbo-q5_0.gguf。下載 qwen2.5-3b-instruct-q4_k_m.gguf。將這些模型放置於 Electron 應用的 resources/models 目錄下。音訊管線搭建：在主進程中初始化 portaudio 錄音流（採樣率 16kHz，單聲道）。將音訊流接入 Silero VAD。設定狀態機：IDLE -> LISTENING (VAD active) -> PROCESSING (VAD silence > 500ms)。推論管線實作：階段一（聽）：當狀態轉為 PROCESSING，將緩衝區音訊傳送給 Whisper 實例。階段二（想）：獲取 Whisper 的輸出字符串，結合當前選定的 System Prompt，調用 llama.prompt()。階段三（寫）：獲取 LLM 的輸出，使用 robotjs.typeString() 或 nut.js 模擬鍵盤輸入到當前視窗。性能優化技巧：預熱（Warm-up）：應用啟動時，先對兩個模型進行一次空推論，以確保權重已載入記憶體且計算圖已構建。模型卸載（Unloading）：為了節省資源，可設定計時器。若使用者 10 分鐘未進行語音輸入，自動釋放 LLM 佔用的記憶體，待下次喚醒時再重新載入（雖然會有幾秒延遲，但能顯著降低背景資源佔用）。KV Cache 管理：在 node-llama-cpp 中利用 KV Cache，對於多輪對話或連續修改指令，可以極大加速推論速度。7.3 使用者介面（UI/UX）設計為了模仿 Typeless 的流暢感，UI 應盡量「隱形」。懸浮球（Floating Widget）：設計一個極小的懸浮視窗，僅顯示麥克風狀態與波形。鬼影文字（Ghost Text）：在 LLM 思考時，可以先將 Whisper 的原始文本以灰色字體顯示在懸浮窗上，待 LLM 處理完畢後，再將最終結果輸入目標視窗。這能有效降低使用者的「心理延遲感」。8. 結論與展望8.1 研究總結本報告證實了構建「Local Typeless」系統不僅在技術上可行，而且在當前的開源生態下，能夠達到極高的完成度。透過整合 Electron 的跨平台能力、Whisper Large-v3-Turbo 的極致聽覺識別、以及 Qwen 2.5 3B 的強大邏輯思維，我們能夠在消費級硬體上實現一個完全隱私、零數據外洩且高度智慧的語音重寫助手。混合模式（Hybrid Mode）的應用是其中的關鍵。它解決了單一模型無法兼顧「聽」與「想」的困境，並透過本地硬體的直接調用消除了網路延遲的不確定性。對於需要處理中英文混合內容的專業用戶而言，這套系統甚至能提供比雲端服務更精準的客製化體驗。8.2 未來展望隨著 NPU（神經網路處理器）在 PC 與 Mac 上的全面普及，未來的 Local Typeless 將更加高效。我們預見以下趨勢：常駐聆聽（Always-on Listening）：藉由超低功耗的 NPU，系統可以全天候運行 VAD，實現真正的「隨叫隨到」而無損續航。設備端持續學習（On-device Continual Learning）：未來的本地 LLM 將能通過 LoRA（Low-Rank Adaptation）技術，根據使用者的修改記錄自動微調，越用越懂使用者的語氣與常用詞彙。這不僅是一個工具的開發，更是對「個人計算」主權的一次重申。透過掌握本地 AI 技術，我們將重新定義人機互動的邊界。表 1：推薦模型與硬體需求對照表硬體規格 (RAM)ASR 模型推薦LLM 模型推薦適用場景8 GBWhisper Base.en (75MB)Gemma-2-2B (Q4)純英文快速紀錄、創意發想16 GBWhisper Large-v3-Turbo (Q5)Qwen 2.5-3B-Instruct (Q4)最佳推薦。中英混合、專業商務寫作、程式開發32 GB+Whisper Large-v3Qwen 2.5-7B-Instruct (Q4)極致精準度、複雜邏輯推理、長文本重構表 2：提示工程指令對照表（Prompt Matrix）功能需求系統指令 (System Prompt Instruction)關鍵詞去冗餘 (Typeless Core)"Remove filler words, false starts, and repetitions. Maintain original meaning and tone."Keep Meaning, Remove Fillers程式碼轉錄"Transcribe as code logic. Convert spoken variable names to snake_case or camelCase. Use Markdown blocks."Formatting, Code Logic中英混合優化"Preserve code-switching. Ensure grammatical flow between Chinese and English. Add spacing between languages."Bilingual, Flow, Spacing(本報告基於 2026 年 1 月的最新開源技術與研究資料彙編而成，所有提及之模型與工具均可於 Hugging Face 及 GitHub 取得。)